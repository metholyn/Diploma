# Розробка Веб-застосунку для бібліотеки

**Дипломник:** Переверзов Марк Андрійович (6.1212-пзс)  
**Тема:** Розробка Веб-застосунку для бібліотеки з інтеграцією Spring Boot та Next.js / Web application development for a library with Spring Boot and Next.js integration

## Архітектура та Технології

- **Backend:** Spring Boot (REST API, Spring Data JPA, Validation, Spring Security)
- **Frontend:** Next.js (App Router, SSR/SSG, API routes)
- **База даних:** MySQL
- **Тип взаємодії:** REST API

---

## ToDo (План розробки)

### 1. Ініціалізація та інфраструктура
- [x] Ініціалізувати базу даних MySQL.
- [x] Створити базовий бекенд-проєкт Spring Boot (маршрутизація, підключення до БД, налаштування середовища).
- [x] Створити базовий фронтенд-проєкт Next.js.
- [x] Налаштувати CORS та базову комунікацію між бекендом і фронтендом.

### 2. Проєктування бази даних та моделей (Spring Data JPA)
- [x] Сутність `User` (користувач).
- [x] Перерахування або сутність `Role` (читач, бібліотекар / адміністратор).
- [x] Сутність `Book` (книга для каталогу).
- [x] Розширити `Book`: додати поля `category`, `description`, `language`.
- [x] Сутність `UserCard` (картка користувача).
- [x] Сутність `BorrowRecord` (облік видачі та повернення книг).
- [x] Сутність `OperationHistory` (структура сутності визначена).

### 3. Розробка Backend (Spring Boot)
- [x] Налаштування Spring Security та JWT-авторизації.
- [x] Реалізація авторизації та управління ролями (реєстрація, логін).
- [x] Реалізація REST-контролерів для каталогу (CRUD книг, пошук за назвою).
- [x] Винести бізнес-логіку у сервісний шар (`@Service`-класи: `BookService`, `UserService`, `BorrowService`).
- [x] Додати анотації валідації (`@Valid`, `@NotNull`, `@Min`) до DTO запитів.
- [x] Реалізувати глобальний обробник виключень (`@RestControllerAdvice` — `GlobalExceptionHandler`).
- [x] Реалізувати пагінацію та сортування для книг (`Spring Data Pageable`, `Page<Book>`).
- [x] Реалізувати фільтрацію книг за назвою/автором (параметр `query`), доступністю (`available`), категорією.
- [x] Реалізація API управління читачами (отримання списку, створення та перегляд картки).
- [x] Реалізувати API блокування/розблокування читача (`PATCH /api/users/{id}/status`).
- [x] Реалізувати ендпоінт профілю поточного користувача (`GET /api/users/me`).
- [x] Реалізація API видачі та повернення книг (перевірка доступності, фіксація дат).
- [x] Інтегрувати запис в `OperationHistory` при видачі та поверненні книги (`BorrowService`).

### 4. Розробка Frontend (Next.js)
- [x] Налаштування App Router та системи захищених маршрутів (Protected Routes).
- [x] Сторінки авторизації та реєстрації.
- [x] Головна сторінка та сторінка каталогу (пошук за назвою та автором, фільтр «тільки доступні»).
- [x] Зробити посилання «View Details» у каталозі — перехід на сторінку деталей книги.
- [x] Реалізувати сторінку детальної інформації про книгу (`/catalog/[id]`).
- [x] Реалізувати пагінацію у каталозі та в адмін-панелі книг.
- [x] Панель бібліотекаря/адміністратора:
    - [x] Управління читачами (перегляд списку, створення картки).
    - [x] Блокування/розблокування читача в адмін-панелі.
    - [x] Каталог книг: додавання, редагування (форма PUT) та видалення книг.
    - [x] Оформлення видачі та повернення книг.
    - [x] Сторінка перегляду історії операцій (`/admin/history`).
- [x] Особистий кабінет читача: поточні видачі, особиста історія (`/profile`).
- [x] Замінити `alert()` у обробці помилок на Toast-компонент (`components/Toast.tsx`).

### 5. Тестування та фіналізація
- [x] Написати unit-тести сервісного шару (`BookServiceTest`, `BorrowServiceTest` з Mockito).
- [ ] E2E тестування ключових потоків (пошук → видача → повернення → перегляд у кабінеті).
- [ ] Перевірити RBAC: доступність функцій відповідно до ролей (READER, LIBRARIAN, ADMIN).
- [ ] Розгортання застосунку (налаштувати `application.properties` для prod-середовища, Docker або хмарний хостинг).
- [ ] Підготовка до демонстрації.
